import { useArtworkLayout } from './useArtworkLayout';

export { useArtworkLayout };

interface ArtworkLayoutOptions {
  width: number;
  frameHeight: number;
  imageHeight: number;
  imageTop?: number;
}

export type { ArtworkLayoutOptions };
